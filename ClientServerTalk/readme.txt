******************************************************************************************************************************************
Copy all the files to a directory in linux
type the command > make
all the executable will be created
type the command > ./server
the command above will run the server executable
type the command > ./client <IP address>
the command above will run the client and connect to the desired IP address
type the command > make clean
the above command will remove all the .o files and executable created
******************************************************************************************************************************************